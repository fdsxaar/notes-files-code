sirego主从复制考量：
1.从服务器只处理读命令请求，不处理修改命令请求，所以，从服务器应
该有这种请求的程序逻辑

主从复制过程：
向从服务器发送slaveof host port 

从服务器向主服务器发送psync ? -1 

//维持一个缓冲，在生成和发送rdb文件时，临时缓冲进来的命令请求

主服务器生成rdb文件
func genRDB(){
    //拷贝一份hashmap
    //生成一个rdb文件
}

//发送rdb文件
func sendRDBToRepl(){
    
}

//命令广播
func cmdBroadcast(){

}

//固定长度先进先出积压缓冲区,用于命令传播
一个积压缓冲区 保存命令 
此时主服务器的offset